# Project 7 Data

The committed benchmark contains 40 synthetic cases: benign, incomplete,
unauthorized delegation, malicious retrieved documents, poisoned tool metadata,
duplicate delivery, canary-exfiltration attempts, and schema-invalid payloads.
Every category has five cases and the split is deterministic.
